name: liverpool_analytics
version: '1.0.0'
profile: liverpool

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  liverpool_analytics:
    staging:
      materialized: view
    marts:
      materialized: table
